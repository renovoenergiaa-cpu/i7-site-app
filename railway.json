{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd apps/api && npx prisma generate && cd ../.. && npm --prefix apps/api run start"
  }
}
